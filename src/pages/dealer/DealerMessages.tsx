import Messages from "@/pages/Messages";

const DealerMessages = () => {
  return (
    <div className="h-full">
      <Messages hideNavbar={true} />
    </div>
  );
};

export default DealerMessages;